"""
QField export module for Linear Geoscience Mapping Tools.

Absorbed from the standalone "LGS QField Exporter" plugin. Exports the current
QGIS project for QField with selectable layers and an offline-only (cable
transfer) workflow.

Launched from the main plugin via ``run_qfield_export`` in mainplugin.py, which
opens ``qfield_export.gui.export_dialog.ExportDialog``.
"""
